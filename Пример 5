#include <stdlib.h> 
#include <stdio.h> 
int main() 
{ 
	char *some_memory = (char*)0; 
	printf("A read from null %sn", some_memory); 
	printf(some_memory, "A write to null"); 
	exit(EXIT_SUCCESS); 
}
